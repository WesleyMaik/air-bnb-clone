import styled from "styled-components";

const Footer = () => {
    const Footer = styled.footer`
        display:flex;
    `;

    return(
        <Footer>

        </Footer>
    )
}

export default Footer;